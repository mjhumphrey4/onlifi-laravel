plugins {
    id("com.android.application")
}

android {
    namespace = "net.onlifi.manager"
    compileSdk = 35

    def hasReleaseSigning = project.findProperty("ONLIFI_RELEASE_STORE_FILE") != null
            && project.findProperty("ONLIFI_RELEASE_STORE_PASSWORD") != null
            && project.findProperty("ONLIFI_RELEASE_KEY_ALIAS") != null
            && project.findProperty("ONLIFI_RELEASE_KEY_PASSWORD") != null

    defaultConfig {
        applicationId = "net.onlifi.manager"
        minSdk = 23
        targetSdk = 35
        versionCode = 1
        versionName = "1.0.0"

        buildConfigField "String", "ONLIFI_API_BASE_URL", "\"${project.findProperty("onlifiApiBaseUrl") ?: "https://api.onlifi.net/api"}\""
    }

    buildFeatures {
        buildConfig = true
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    signingConfigs {
        release {
            def storeFilePath = project.findProperty("ONLIFI_RELEASE_STORE_FILE")
            def storePasswordValue = project.findProperty("ONLIFI_RELEASE_STORE_PASSWORD")
            def keyAliasValue = project.findProperty("ONLIFI_RELEASE_KEY_ALIAS")
            def keyPasswordValue = project.findProperty("ONLIFI_RELEASE_KEY_PASSWORD")

            if (storeFilePath != null) {
                storeFile file(storeFilePath)
            }
            storePassword = storePasswordValue
            keyAlias = keyAliasValue
            keyPassword = keyPasswordValue
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            shrinkResources false
            if (hasReleaseSigning) {
                signingConfig signingConfigs.release
            }
        }
        debug {
            applicationIdSuffix = ".debug"
            versionNameSuffix = "-debug"
        }
    }
}
